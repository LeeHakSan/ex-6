# 제출 자료 캡처 가이드

`REQUIREMENTS_CHECKLIST.md`에서 "🔲 직접 확인 필요" / "⛔"로 표시된 항목을 한 번에 처리하는 순서입니다.
위에서부터 순서대로 하면 왔다 갔다 안 하고 한 번에 끝납니다. 스크린샷은 `docs/screenshots/` 폴더 같은 곳에
파일명 붙여서 모아두면 제출할 때 편합니다 (폴더는 아무 데나 본인 편한 곳이어도 됩니다).

---

## 0. 더미 데이터 지우기 (제출 직전에 하거나, 지금 미리 해도 됨)

Supabase SQL Editor에서:
```sql
delete from plans where title like '더미:%';
```
(cascade 설정이라 거기 딸린 할 일·실행 기록도 같이 지워집니다)

> 지금 바로 지우고 아래 1~9번을 **전부 실제 데이터로** 진행하는 걸 추천합니다.
> 그러면 스크린샷 한 번으로 "기능 확인"과 "C78~C81 실데이터 요건"을 동시에 만족시킬 수 있어요.

---

## 1. 진짜 계획 만들기 → 캡처
1. **계획** 탭에서 지금 실제로 하고 있는 일 1개로 계획 생성 (제목/기간/우선순위/성공 기준/예상 시간 채우기)
2. 저장 후 "내 계획 목록"에 뜬 카드 캡처 → `01_plan_created.png`
   - 이 캡처 하나로 **C04, C05, C06, C07** 근거가 됨

## 2. 계획 수정 → 수정 이력 확인 → 캡처
1. 방금 만든 계획의 **수정** 클릭 → 아무 값이나 하나 바꾸고 저장 (예: 예상 시간 변경)
2. 같은 카드에서 **수정 이력 보기** 클릭
3. 이전 값이 남아있는 걸 캡처 → `02_plan_revision_history.png`
   - **C08** 근거

## 3. 진짜 할 일 5개 이상 만들기 → 캡처
1. **할 일** 탭에서 위 계획을 선택한 채로, 할 일을 5개 이상 생성 (제목/마감일/우선순위/태그/예상 시간 채우기)
   - 최소 1개는 마감일을 **오늘 이전**으로 설정 (지연 테스트용)
   - 나중에 삭제 테스트할 것까지 감안해 6개 만들어도 됨
2. 목록 캡처 → `03_todos_created.png`
   - **C09, C14, C15, C16, C17** 근거

## 4. 할 일 수정 / 완료 / 되돌리기 / 삭제 → 캡처
1. 할 일 하나 **수정** → 캡처 `04_todo_edited.png` (**C10**)
2. 다른 할 일 하나 **완료** 클릭 → 완료 배지 캡처 `05_todo_completed.png` (**C11**)
3. 그 할 일 **되돌리기** 클릭 → 다시 진행중으로 바뀐 것 캡처 `06_todo_reverted.png` (**C12**)
4. (5개는 남기고) 여분 할 일 하나 **삭제** → 목록에서 사라진 것 캡처 `07_todo_deleted.png` (**C13**)

## 5. 완료 연타 멱등성 테스트 → 캡처 (중요, 채점 포인트)
1. 진행 중인 할 일 하나의 **완료** 버튼을 아주 빠르게 두 번 클릭
2. 완료 배지가 1개만 붙는지 캡처 `08_complete_double_click.png`
3. **돌아보기** 탭으로 이동 → 완료 수가 (더블클릭 전 대비) 정확히 +1인지 캡처 `09_review_count_after_double_click.png`
   - **C21, C22** 근거

## 6. 검색 / 필터 / 정렬 → 캡처
1. 검색창에 아무 할 일 제목 일부 입력 → 결과 좁혀지는 것 캡처 `10_search.png` (**C18**)
2. 상태 필터를 "완료"로 바꿔서 결과 캡처 `11_filter.png` (**C19**)
3. 정렬 기준을 바꿔보고, 화면의 "정렬 기준: ..." 문구 + 실제 정렬된 목록이 같이 보이게 캡처 `12_sort.png` (**C20**)

## 7. 실행 기록 3개 이상 → 캡처
1. 할 일 하나를 열어서(**실행 기록/상세**) 실행 기록을 3개 이상 추가 (시작/끝 시각, 그 중 1개는 "막혔던 이유"도 입력)
2. 모달에 기록들이 그 할 일에 붙어서 쌓이는 것 캡처 `13_execution_logs.png`
   - **C23, C24, C25, C26, C27, C80** 근거
3. 실행 기록 추가 후 계획 목록으로 돌아가서 예상 시간 값이 그대로인지 확인 (덮어쓰기 안 됨)

## 8. 돌아보기 집계 + 드릴다운 → 캡처
1. **돌아보기** 탭에서 계획 수/완료/지연/막힘/예상·실제 시간/차이 숫자 전체 캡처 `14_review_summary.png` (**C28~C32, C81**)
2. "막힘" 숫자 클릭 → 할 일 탭으로 이동하며 막힌 할 일만 걸러지는 것 캡처 `15_drilldown_blocked.png` (**C83**)
3. "완료"나 "지연" 숫자도 한 번씩 눌러서 필터링되는지 확인

## 9. 고칠 점 → 다음 계획 이월 → 캡처
1. 돌아보기 탭 아래 "고칠 점" 텍스트 작성 후 저장
2. **계획** 탭으로 이동 → "새 계획 만들기" 폼 열기
3. "이전 계획에서 넘어온 메모"에 방금 쓴 문구가 자동으로 채워져 있는 것 캡처 `16_carried_over_note.png`
   - **C33** 근거 (이번엔 실제 UI 흐름으로 테스트하는 것 — SQL로 넣은 더미 말고)

## 10. 스크립트 방어(XSS) → 캡처
1. 아무 할 일 제목에 `<script>alert('xss')</script>` 입력해서 저장
2. 알림창이 안 뜨고, 글자가 그대로(꺾쇠 포함 텍스트로) 보이는 것 캡처 `17_xss_defense.png` (**C57**)
3. 확인 후 그 할 일은 지워도 됨

## 11. 비밀키 점검 → 캡처
1. 배포된 사이트에서 브라우저 개발자도구(F12) → **콘솔(Console)** 탭 → 에러/경고 없는지, `service_role` 같은 단어가 안 보이는지 확인
2. 개발자도구 → **네트워크(Network)** 탭 → Supabase로 가는 요청 하나 클릭 → 요청 헤더에 `apikey`가 anon key인지 확인(= `js/supabaseClient.js`에 넣은 값과 동일한지) → `service_role`이 아닌지
3. 결과 캡처 `18_secret_check.png` (**C58**)

## 12. 내보내기 + 새로고침 → 캡처
1. 헤더의 **내 데이터 내보내기** 클릭 → JSON 파일 다운로드 확인
2. 다운로드된 파일 캡처 `19_export_file.png` (**C36**)
3. 브라우저 새로고침(F5) → 계획/할 일/돌아보기 숫자가 그대로인지 캡처 `20_after_refresh.png` (**C35**)

## 13. 공개 배너 + 로그인 없는 접근 → 캡처
1. 배포 URL을 **새 시크릿 창**으로 열기
2. 로그인 화면 없이 바로 앱이 뜨고, 상단에 공개 안내 배너가 보이는 것 캡처 `21_incognito_public.png` (**C01, C82**)

---

## 다 끝나면
1. `REQUIREMENTS_CHECKLIST.md`에서 이 가이드로 확인한 항목들을 `[x]`로 바꾸기
2. `README.md`의 "AI 활용 내역" 문단이 실제로 한 것과 맞는지 다시 한번 읽어보고 필요하면 수정
3. 커밋해서 GitHub에 반영 (원하면 저한테 캡처 결과 알려주시면 체크리스트 갱신과 커밋은 제가 해드릴게요)
